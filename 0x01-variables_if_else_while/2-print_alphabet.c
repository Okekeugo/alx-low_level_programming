#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success/Correction)
 */
int main(void)
{
	char alphabet;

	for (alphabet = 'a'; alphabet <= 'z'; alphabet(++)
			{ putcgar(alphabet);
			}

		putchar('\n');
	return (0);
}
